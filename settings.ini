# The expression decides which variables exist.
formula = clamp(base * (1 + rate)^years + sin(t), 0, limit)

# Values are data, not hard-coded C++.
[variables]
base = 1000.0
rate = 0.05
years = 10
t = 0.5
limit = 2000.0
